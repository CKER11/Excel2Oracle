模板配置规则如下：
1.于A1结尾处配置数据源：例如：标题(ds1=oracle表1；ds2=表2)；
2.配置普通一一对应的数据：例如：ds1.dtm；
3.配置向下自增的数据，在任意字段后加"[down]"：例如：ds1.P1[down]，所有自增配置必须处于一行，以下的所有数据源读取位置行加对应自增数据条数；
4.配置向右自增的数据，在任意字段后加"[right]"：例如：ds1.P1[right]，所有自增配置必须处于一列，以右的所有数据源读取位置列加对应自增数据条数；
5.配置向下自增或向右自增的公共字段(合并单元格)，在改字段后加"[public]"；
6.配置向下自增或向右自增的数据的主键(关键列)，在该字段后加[key]，如不加主键默认为第一个读到的非public列；
7.配置向下自增或向右自增的数据的停止方式，有4种方式：
       一、读到主键为空的行即停(默认)；
	   二、读到全为空的行即停，在改字段后加"[allEmpty]"；
	   三、读到包含空的行即停，在改字段后加"[hasEmpty]"；
	   四、读到关键词即停，在改关键词后加(数据集名.downend/rightend)，例如：合计(ds1.downend)
8.配置位置不受自增数据影响的字段，，在该字段后加[fixed]，例如：合计(ds1.P2[fixed])
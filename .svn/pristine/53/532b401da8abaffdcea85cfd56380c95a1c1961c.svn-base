模板配置规则如下：
1.于A1结尾处配置数据源：例如：标题(ds1=oracle表1；ds2=表2)；
2.配置普通一一对应的数据：例如：ds1.dtm；
3.配置向下自增的数据，在任意字段后加"[down]"：例如：ds1.P1[down]，所有自增配置必须处于一行，以下的所有数据源读取位置行加对应自增数据条数；
4.配置向右自增的数据，在任意字段后加"[right]"：例如：ds1.P1[right]，所有自增配置必须处于一列，以右的所有数据源读取位置列加对应自增数据条数；
5.配置向下自增或向右自增的公共字段(合并单元格)，在改字段后加"[public]"；
6.配置向下自增或向右自增的数据的主键(关键列)，在该字段后加[key]，如不加默认主键为第一个读到的非public列；
7.配置向下自增或向右自增的数据的停止方式，有4种方式：
       一、读到主键为空的行即停(默认)；
	   二、读到全为空的行即停，在改字段后加"[allEmpty]"；
	   三、读到包含空的行即停，在改字段后加"[hasEmpty]"；
	   四、读到第一个关键词即停，在改关键词后加(数据集名.downend/rightend)，例如：合计(ds1.downend)；
8.配置位置不受自增数据影响的字段，，在该字段后加[fixed]，例如：合计(ds1.P2[fixed])；
9.配置日期字段，在任意字段后加"[date]",默认格式为"yyyy/MM/dd",如想更改格式，配置为在date后加格式，例如[date("yyyy-MM-dd HH:mm:ss")]  注：yyyy表示年，MM表示月，dd表示日，HH表小时（24小时制），hh表示小时（12小时制），mm表示分钟 ss表示秒；
10.配置导入数据中不存在的Guid列或者序号列，在A1的表定义后加[no=序号列名1&序号列名2,guid=guid列名1&guid列名2]，guid和no使用"，"隔开,多个列名用"&"隔开，Guid列和序号列会在数据预览和导入时加入该列，guid列每行会随机生成一个guid,序号列每行生成该行序号，例如 ：燃料日报（ds1=EI_CZ800202[no=RowNo];ds2=EI_CZ800201[no=rowNo&rowNum,guid=id]）
10.读取数据时限为5秒,超过5秒认为陷入了死循环；